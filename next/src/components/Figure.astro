---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

interface Props {
	src: string | ImageMetadata;
	alt?: string;
	caption?: string;
	resize?: string;
}

const { src, alt, caption } = Astro.props;
const altText = alt ?? caption ?? '';
const isImported = typeof src !== 'string';
---
<figure>
	{isImported ? (
		<Image src={src} alt={altText} loading="lazy" style="max-width: 100%; height: auto;" />
	) : (
		<img loading="lazy" src={src} alt={altText} style="max-width: 100%; height: auto;" />
	)}
	{caption && (
		<figcaption>
			<p set:html={caption} />
		</figcaption>
	)}
</figure>
